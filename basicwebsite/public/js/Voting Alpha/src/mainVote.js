//const { helloWorld } = require('./mainVote1');
import { hello } from 'mainVote1';

function addVote(voterAddress, votedAddress) {
  alert(voterAddress+ ' '+ votedAddress);
  //alert(voterAddress+ ' '+ votedAddress);
}
